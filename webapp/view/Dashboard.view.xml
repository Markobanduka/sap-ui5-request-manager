<mvc:View
    xmlns:mvc="sap.ui.core.mvc"
    xmlns="sap.m"
    xmlns:l="sap.ui.layout"
    controllerName="ui5.requestmanager.controller.Dashboard"
    displayBlock="true">

    <Page title="{i18n>dashboardTitle}">
        <content>
            <l:VerticalLayout class="sapUiContentPadding" width="100%">
                <Title text="{i18n>dashboardWelcome}" level="H2" />
                <Text text="{i18n>dashboardIntro}" class="sapUiSmallMarginBottom" />

                <l:HorizontalLayout allowWrapping="true" class="sapUiSmallMarginTop">

                    <GenericTile
                        header="{i18n>tileCreateHeader}"
                        subheader="{i18n>tileCreateSubheader}"
                        press=".onNavigateToCreate"
                        class="sapUiTinyMargin">
                        <TileContent>
                            <NumericContent value="+" />
                        </TileContent>
                    </GenericTile>

                    <GenericTile
                        header="{i18n>tileMyRequestsHeader}"
                        subheader="{i18n>tileMyRequestsSubheader}"
                        press=".onNavigateToMyRequests"
                        class="sapUiTinyMargin">
                        <TileContent>
                            <NumericContent value="📋" />
                        </TileContent>
                    </GenericTile>

                    <GenericTile
                        header="Statistics"
                        subheader="View analytics"
                        press=".onNavigateToStats"
                        class="sapUiTinyMargin">
                        <TileContent>
                            <NumericContent value="📊" />
                        </TileContent>
                    </GenericTile>
                    <GenericTile
                        header="Notifications"
                        subheader="Configure alerts"
                        press=".onNavigateToNotifications"
                        class="sapUiTinyMargin">
                        <TileContent>
                            <NumericContent value="🔔" />
                        </TileContent>
                    </GenericTile>

                    <GenericTile
                        header="Help"
                        subheader="Get assistance"
                        press=".onHelp"
                        class="sapUiTinyMargin">
                        <TileContent>
                            <NumericContent value="?" />
                        </TileContent>
                    </GenericTile>

                </l:HorizontalLayout>
            </l:VerticalLayout>
        </content>
    </Page>
</mvc:View>